import styled from 'styled-components'

export const Button = styled.button`
    border: none;
    border-radius: 5px;
    padding: 10px 15px;
    text-align: center;

    &:focus,
    &:hover {
        border: none;
        border-radius: 5px;
    }
`
